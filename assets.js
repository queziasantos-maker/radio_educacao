// Asset Map (Read-Only)
// Edit assets in the Images tab.
// Use these IDs with the runtime helpers:
//
//   const sprite = lib.image("monster_truck");
//   ctx.drawImage(sprite, x, y);
//
//   const rec = lib.getAsset("monster_truck");
//   // { id, url, type, aspect_ratio }

const assets = {};

// No assets yet — generate one from the Images tab.

